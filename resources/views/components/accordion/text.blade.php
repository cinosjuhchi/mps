<p>
    {{ $slot }}

    <span class="badge text-bg-primary mx-1">{{ $code }}</span>
    <span class="badge text-bg-danger mx-1">{{ $jawatan }}</span>
    <span class="badge text-bg-success mx-1">{{ $name }}</span>
</p>
